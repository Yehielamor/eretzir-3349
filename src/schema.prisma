CREATE TABLE users (id uuid PRIMARY KEY, email text, created_at timestamptz DEFAULT now()); CREATE TABLE games (id uuid PRIMARY KEY, room_code text, letter char, start_time timestamptz, status text); CREATE TABLE scores (id uuid PRIMARY KEY, user_id uuid REFERENCES users(id), game_id uuid REFERENCES games(id), points integer, words_submitted jsonb);